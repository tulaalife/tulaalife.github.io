<section class="section section-tools bg-subtle">
    <div class="container grid-layout">
        <div class="text-col">
            <div class="badge-small">All-in-One Wellness</div>
            <h2 class="section-title">
                Replace 5 Apps <br />
                <span class="text-gradient">With One.</span>
            </h2>
            <p class="body-text">
                Simplify your digital wellness. Tulaa brings your essential
                health tools into one secure, offline-first ecosystem.
                <strong>No data mining. No subscriptions.</strong>
            </p>

            <div class="tools-grid">
                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">🌸</span>
                        <strong>Cycle Tracker</strong>
                    </div>
                    <p>Predict periods & symptoms.</p>
                </div>

                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">⚡️</span>
                        <strong>Rhythm</strong>
                    </div>
                    <p>Build healthy habit streaks.</p>
                </div>

                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">💧</span>
                        <strong>Hydration</strong>
                    </div>
                    <p>Monitor daily water intake.</p>
                </div>

                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">⏳</span>
                        <strong>Fasting</strong>
                    </div>
                    <p>Track fasting time windows.</p>
                </div>

                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">💡</span>
                        <strong>Health Tip</strong>
                    </div>
                    <p>Get research-backed tips.</p>
                </div>

                <div class="tool-card">
                    <div class="card-header">
                        <span class="emoji">📚</span>
                        <strong>Therapy Blog</strong>
                    </div>
                    <p>Learn expert care routines.</p>
                </div>
            </div>
        </div>

        <div class="visual-col">
            <img
                src="/images/trackers-collage.webp"
                alt="Tulaa App Interface showing Cycle, Water, and Habit trackers"
                class="feature-img shadow-lg"
                loading="lazy"
            />
        </div>
    </div>
</section>

<style>
    /* --- SECTION LAYOUT --- */
    .section {
        padding: 100px 0;
    }

    .grid-layout {
        display: grid;
        grid-template-columns: 1fr 1fr; /* 50/50 split on Desktop */
        gap: 80px;
        align-items: center;
    }

    /* --- TYPOGRAPHY --- */
    .badge-small {
        display: inline-block;
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        color: var(--primary);
        background: rgba(var(--primary-rgb), 0.1);
        padding: 6px 12px;
        border-radius: 99px;
        margin-bottom: 16px;
    }

    .section-title {
        font-size: 2.5rem; /* Adjustable via variable */
        line-height: 1.1;
        margin-bottom: 24px;
        font-weight: 700;
    }

    .body-text {
        font-size: 1.1rem;
        color: var(--text-muted);
        margin-bottom: 32px;
        line-height: 1.6;
        max-width: 480px;
    }

    /* --- COMPACT GRID (The "Tools") --- */
    .tools-grid {
        display: grid;
        /* 3 Columns by default for compactness */
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
    }

    .tool-card {
        background: var(--bg-main, #ffffff);
        padding: 12px; /* Compact padding */
        border-radius: 16px;
        border: 1px solid var(--border, #e5e7eb);
        transition: all 0.2s ease;

        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    .card-header {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 6px;
    }

    .tool-card .emoji {
        font-size: 1.2rem;
        line-height: 1;
    }

    .tool-card strong {
        font-size: 0.95rem;
        font-weight: 600;
        color: var(--text-main);
    }

    .tool-card p {
        font-size: 0.8rem; /* Smaller text for descriptions */
        color: var(--text-muted);
        margin: 0;
        line-height: 1.3;
    }

    /* --- IMAGE STYLING --- */
    .visual-col {
        position: relative;
    }

    /* --- RESPONSIVE DESIGN --- */

    /* Tablet: Switch grid to 2 columns so cards don't get too squished */
    @media (max-width: 1024px) {
        .tools-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    /* Mobile */
    @media (max-width: 768px) {
        .section {
            padding: 64px 0;
        }

        .grid-layout {
            grid-template-columns: 1fr; /* Stack visually */
            gap: 40px;
            text-align: center;
        }

        /* Reorder: Visual first, or Text first? 
           Usually "Text First" is better for SEO/Reading, 
           but "Visual First" is better for engagement. 
           Let's keep Text First here as the headline is strong. */

        .text-col {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .tools-grid {
            width: 100%;
            /* Keep 2 columns on mobile for "App Store" feel */
            grid-template-columns: repeat(2, 1fr);
            gap: 12px;
            text-align: left; /* Keep card text left-aligned for readability */
        }

        .tool-card {
            padding: 14px;
        }
    }
</style>
