---
// src/components/YogaSection.astro
import SectionCTA from "../SectionCTA.astro";
import {
    YOGA_UI,
    CATEGORY_MAP,
    type SupportedLang,
} from "../../utils/ui-strings";
import { yogaPlans } from "../../data/generated";

const currentLang = (Astro.currentLocale || "en") as SupportedLang;
const t = YOGA_UI[currentLang] || YOGA_UI.en;

const displayPlans = yogaPlans.filter((p) => p.language === currentLang);
---

<section class="scroll-section section-yoga">
    <div class="content-wrapper">
        <!-- 1. Pure Header Container Grid Alignment Module -->
        <div class="container">
            <div class="section-header-left text-col animate-on-scroll">
                <h2 class="section-title">
                    <span class="title-main">{t.heroTitleTop}</span>
                    <br />
                    <span class="text-gradient">{t.heroTitleBottom}</span>
                </h2>
                <p class="body-text">{t.heroBody}</p>
            </div>
        </div>

        <!-- 2. Filter Navigation Control Module (Label Removed for Minimalist Look) -->
        <div class="container filter-container animate-on-scroll delay-1">
            <div class="filter-scroll">
                <button class="filter-btn active" data-filter="all"
                    >{t.filterAll}</button
                >
                <button class="filter-btn" data-filter="general"
                    >{t.filterGeneral}</button
                >
                <button class="filter-btn" data-filter="womens"
                    >{t.filterWomens}</button
                >
                <button class="filter-btn" data-filter="seniors"
                    >{t.filterSeniors}</button
                >
                <button class="filter-btn" data-filter="medical"
                    >{t.filterMedical}</button
                >
            </div>
        </div>

        <!-- 3. Full Width Horizontal Carousel Container Track -->
        <div class="container carousel-wrapper animate-on-scroll delay-2">
            <div class="carousel-track" id="featured-plans-track">
                {
                    displayPlans.map((p) => (
                        <a
                            href={`/${currentLang}/yoga/${p.slug}`}
                            class="carousel-card"
                            data-goals={p.goals?.join(",")}
                        >
                            <div class="card-image">
                                <img
                                    src={p.image}
                                    alt={p.title}
                                    loading="lazy"
                                    decoding="async"
                                />
                            </div>
                            <div class="card-content">
                                <h3 class="card-title">{p.title}</h3>
                                {/* Subtitle description paragraph removed entirely */}
                                <div class="card-footer">
                                    <span class="view-link">{t.view}</span>
                                    <span class="icon">→</span>
                                </div>
                            </div>
                        </a>
                    ))
                }
            </div>
        </div>

        <!-- 4. Left-Aligned Unified Secondary Button Hook -->
        <div class="container cta-wrapper animate-on-scroll delay-2">
            <SectionCTA
                href={`/${currentLang}/yoga`}
                labelEn="Explore All Plans"
                labelHi="सभी प्लान देखें"
            />
        </div>
    </div>
</section>

<script is:inline define:vars={{ CATEGORY_MAP }}>
    document.addEventListener("astro:page-load", () => {
        const filterBtns = document.querySelectorAll(".filter-btn");
        const track = document.getElementById("featured-plans-track");
        if (!track || filterBtns.length === 0) return;

        const cards = Array.from(track.querySelectorAll(".carousel-card"));

        filterBtns.forEach((btn) => {
            btn.addEventListener("click", () => {
                filterBtns.forEach((b) => b.classList.remove("active"));
                btn.classList.add("active");

                const filterId = btn.getAttribute("data-filter");

                cards.forEach((card) => {
                    if (filterId === "all") {
                        card.style.display = "flex";
                        setTimeout(() => (card.style.opacity = "1"), 10);
                        return;
                    }

                    const targetGoals = CATEGORY_MAP[filterId] || [];
                    const itemGoals = (
                        card.getAttribute("data-goals") || ""
                    ).split(",");
                    const isMatch = targetGoals.some((goal) =>
                        itemGoals.includes(goal),
                    );

                    if (isMatch) {
                        card.style.display = "flex";
                        setTimeout(() => (card.style.opacity = "1"), 10);
                    } else {
                        card.style.opacity = "0";
                        setTimeout(() => (card.style.display = "none"), 300);
                    }
                });

                track.scrollTo({ left: 0, behavior: "smooth" });
            });
        });
    });
</script>

<style>
    .section-yoga {
        width: 100%;
        display: flex;
        align-items: center;
        padding-top: var(--section-v-padding);
        padding-bottom: var(--section-v-padding);
    }

    .content-wrapper {
        display: flex;
        flex-direction: column;
        width: 100%;
        margin: 0 auto;
    }

    .text-col {
        margin-bottom: var(--space-md);
        text-align: left;
        width: 100%;
    }

    .section-title {
        font-size: clamp(2.2rem, 5.5vw, 3.8rem);
        letter-spacing: -0.035em;
        line-height: 1.1;
        text-align: left;
        color: var(--text-main);
    }

    .body-text {
        font-size: clamp(1.05rem, 2vw, 1.2rem);
        color: var(--text-muted);
        max-width: 600px;
        text-align: left;
        line-height: 1.6;
        margin-top: var(--space-xs);
    }

    /* --- FILTER NAVIGATION (CLEAN CHIPS) --- */
    .filter-container {
        display: flex;
        align-items: center;
        margin-bottom: var(--space-md);
        width: 100%;
    }

    .filter-scroll {
        display: flex;
        gap: var(--space-xs);
        overflow-x: auto;
        scrollbar-width: none;
        -webkit-overflow-scrolling: touch;
        padding: 4px 0;
    }

    .filter-scroll::-webkit-scrollbar {
        display: none;
    }

    .filter-btn {
        background: var(--bg-sub);
        border: 1px solid var(--border);
        color: var(--text-muted);
        padding: 6px 16px;
        border-radius: 99px;
        font-size: 0.85rem;
        font-weight: 600;
        cursor: pointer;
        white-space: nowrap;
        transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .filter-btn:hover {
        border-color: color-mix(in srgb, var(--primary) 40%, var(--border));
        color: var(--text-main);
    }

    .filter-btn.active {
        background: var(--primary);
        color: var(--bg-main);
        border-color: var(--primary);
    }

    /* --- HORIZONTAL CAROUSEL --- */
    .carousel-wrapper {
        position: relative;
        width: 100%;
        margin-bottom: var(--space-md);
    }

    /* Polished, zero-obstrusive right edge gradient mask seen in Screenshot 2026-07-06 at 9.58.18 AM.jpg */
    .carousel-wrapper::after {
        content: "";
        position: absolute;
        top: 0;
        right: 0;
        width: 48px;
        height: 100%;
        background: linear-gradient(
            to left,
            var(--bg-main) 0%,
            transparent 100%
        );
        pointer-events: none;
        z-index: 5;
    }

    .carousel-track {
        display: flex;
        gap: var(--space-md);
        overflow-x: auto;
        scroll-snap-type: x mandatory;
        padding: 6px 0 var(--space-sm) 0;
        scrollbar-width: none;
        -webkit-overflow-scrolling: touch;
    }

    .carousel-track::-webkit-scrollbar {
        display: none;
    }

    .carousel-card {
        scroll-snap-align: start;
        flex: 0 0 290px;
        display: flex;
        flex-direction: column;
        background: var(--bg-sub);
        border: 1px solid var(--border);
        border-radius: var(--radius-lg);
        overflow: hidden;
        text-decoration: none;
        color: inherit;

        /* Enforces matching static heights across all filtered states */
        height: 100%;
        min-height: 250px;

        transition:
            transform 0.4s cubic-bezier(0.16, 1, 0.3, 1),
            box-shadow 0.4s ease,
            border-color 0.4s ease;
    }

    .carousel-card:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow);
        border-color: color-mix(in srgb, var(--primary) 30%, var(--border));
    }

    .card-image {
        position: relative;
        aspect-ratio: 16 / 10;
        overflow: hidden;
        border-bottom: 1px solid var(--border);
    }

    .card-image img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .card-content {
        padding: var(--space-md);
        display: flex;
        flex-direction: column;
        gap: var(--space-md);
        flex-grow: 1;
        justify-content: space-between; /* Pushes footer neatly to base */
    }

    .card-title {
        font-size: 1.2rem;
        font-weight: 750;
        margin: 0;
        color: var(--text-main);
        letter-spacing: -0.015em;
        line-height: 1.35;

        /* Enforces a 2-line maximum constraint with fluid ellipsis (...) */
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
        text-overflow: ellipsis;

        /* Reserves a locked height footprint equal exactly to 2 lines of prose */
        min-height: calc(1.35em * 2);
    }

    /* Polished, vertically centered line item alignment */
    .card-footer {
        display: flex;
        align-items: center;
        gap: 6px;
        font-weight: 600;
        color: var(--primary);
        font-size: 0.9rem;
        letter-spacing: 0.01em;
        margin-top: auto;
    }

    .icon {
        display: inline-flex;
        align-items: center;
        transform: translateY(-1px); /* Perfect visual alignment tweak */
        transition: transform 0.2s ease;
    }

    .carousel-card:hover .icon {
        transform: translateX(2px) translateY(-1px);
    }

    .cta-wrapper {
        display: flex;
        justify-content: flex-start;
        margin-top: var(--space-xs);
    }

    /* --- GLOBAL LOCALE OPTIMIZATIONS FOR DEVANAGARI --- */
    :lang(hi) .section-title {
        line-height: 1.35;
    }
    :lang(hi) .filter-btn {
        padding-top: 7px;
        padding-bottom: 5px;
    }
    :lang(hi) .card-title {
        line-height: 1.45;
        min-height: calc(1.45em * 2);
    }

    /* --- RESPONSIVE OPTIMIZATIONS FOR SMALL HEIGHT DEVICES --- */
    @media (max-height: 740px) {
        .text-col {
            margin-bottom: var(--space-sm);
        }
        .body-text {
            display: none;
        }
        .carousel-card {
            flex: 0 0 250px;
        }
        .card-content {
            padding: var(--space-sm);
        }
    }
</style>
