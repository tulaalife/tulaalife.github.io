---
import CTAButton from "./CTAButton.astro";

export interface TipItem {
    tip: string;
    benefit?: string;
    icon?: string; // emoji or URL
}
export interface Props {
    title: string;
    subtitle?: string;
    items: TipItem[]; // ~5 recommended
    intervalMs?: number; // default 5200ms
    ctaHref?: string; // optional CTA (Explore tips)
    ctaLabel?: string;
}

const {
    title,
    subtitle,
    items = [],
    intervalMs = 5200,
    ctaHref,
    ctaLabel,
} = Astro.props;

const hasCTA = Boolean(ctaHref && ctaLabel);
---

{
    items.length > 0 && (
        <section
            class="ts-wrap"
            data-interval={String(intervalMs)}
            tabindex="0"
            role="region"
            aria-label={title}
        >
            <header class="ts-header">
                <h2 class="ts-title">{title}</h2>
                {subtitle && <p class="ts-subtitle">{subtitle}</p>}
            </header>

            <div class="ts-panel" aria-live="polite">
                <ul class="ts-track" role="list">
                    {items.map((t, idx) => (
                        <li
                            class="ts-slide"
                            aria-hidden={idx === 0 ? "false" : "true"}
                            data-active={idx === 0 ? "true" : "false"}
                        >
                            <div class="ts-card">
                                <div class="ts-icon" aria-hidden="true">
                                    {t.icon?.startsWith("http") ? (
                                        <img
                                            src={t.icon}
                                            alt=""
                                            loading="lazy"
                                            decoding="async"
                                        />
                                    ) : (
                                        <span>{t.icon ?? "💡"}</span>
                                    )}
                                </div>
                                <div class="ts-body">
                                    <h3 class="ts-h3">{t.tip}</h3>
                                    {t.benefit && (
                                        <p class="ts-p">{t.benefit}</p>
                                    )}
                                </div>
                            </div>
                        </li>
                    ))}
                </ul>
            </div>

            {hasCTA && (
                <div class="ts-ctaRow">
                    <CTAButton
                        href={ctaHref!}
                        label={ctaLabel!}
                        icon="↗"
                        variant="soft"
                        size="lg"
                    />
                </div>
            )}
        </section>
    )
}

<style>
    .ts-wrap {
        margin: 28px auto 22px;
        padding: 8px 16px 16px;
        max-width: 880px;
    }
    .ts-header {
        text-align: center;
        margin: 0 0 14px;
    }
    .ts-title {
        margin: 0 0 6px;
        font-family: var(--font-sans);
        font-size: var(--fs-h3);
        font-weight: var(--fw-semibold);
        color: var(--fg);
    }
    .ts-subtitle {
        margin: 0;
        font-family: var(--font-sans);
        font-size: var(--fs-body);
        color: var(--muted);
    }

    /* Panel (glassy, subtle glow) */
    .ts-panel {
        --r: 18px;
        margin-top: 14px;
        padding: 14px;
        border-radius: var(--r);
        background: radial-gradient(
                120% 120% at 0% 0%,
                color-mix(in oklab, var(--fg) 6%, transparent),
                transparent 60%
            ),
            color-mix(in oklab, var(--panel) 92%, transparent);
        border: 1px solid color-mix(in oklab, var(--ring) 72%, transparent);
        box-shadow:
            0 1px 0 color-mix(in oklab, var(--fg) 8%, transparent),
            0 12px 28px color-mix(in oklab, var(--fg) 10%, transparent);
        backdrop-filter: saturate(120%) blur(3px);
    }

    /* Slides stack (cross-fade, auto height) */
    .ts-track {
        margin: 0;
        height: var(--ts-h, auto);
        min-height: 132px;
        position: relative;
        list-style: none;
        padding: 0;
    }
    .ts-slide {
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        opacity: 0;
        transform: translateY(8px) scale(0.99);
        transition:
            opacity 0.6s cubic-bezier(0.22, 0.61, 0.36, 1),
            transform 0.6s cubic-bezier(0.22, 0.61, 0.36, 1);
        pointer-events: none;
        will-change: opacity, transform;
    }
    .ts-slide[data-active="true"] {
        opacity: 1;
        transform: translateY(0) scale(1);
        pointer-events: auto;
    }

    .ts-card {
        --r: 16px;
        display: flex;
        gap: 14px;
        align-items: flex-start;
        width: min(100%, 860px);
        padding: 16px;
        border-radius: var(--r);
        font-family: var(--font-sans);
        background: radial-gradient(
                120% 120% at 100% 0%,
                color-mix(in oklab, var(--fg) 5%, transparent),
                transparent 60%
            ),
            color-mix(in oklab, var(--panel) 96%, transparent);
        border: 1px solid color-mix(in oklab, var(--ring) 60%, transparent);
        transition:
            transform 0.25s ease,
            box-shadow 0.25s ease;
    }

    .ts-icon {
        flex: 0 0 auto;
        width: 48px;
        height: 48px;
        border-radius: 12px;
        display: grid;
        place-items: center;
        font-size: 24px;
        background: color-mix(in oklab, var(--btn) 12%, transparent);
        border: 1px solid color-mix(in oklab, var(--ring) 70%, transparent);
    }
    .ts-icon img {
        width: 28px;
        height: 28px;
        object-fit: contain;
    }

    .ts-body {
        flex: 1;
    }
    .ts-h3 {
        margin: 0 0 6px;
        font-family: var(--font-sans);
        font-size: var(--fs-body-lg);
        font-weight: var(--fw-medium);
        color: var(--fg);
    }
    .ts-p {
        margin: 0;
        font-family: var(--font-sans);
        font-size: var(--fs-body-sm);
        line-height: var(--lh-body);
        color: var(--muted);
    }

    .ts-ctaRow {
        display: flex;
        justify-content: center;
        margin: 38px 0 0;
    }
</style>

<script is:inline>
    (() => {
        function boot(root) {
            const track = root.querySelector(".ts-track");
            const slides = Array.from(root.querySelectorAll(".ts-slide"));
            if (!track || slides.length === 0) return;

            const setActive = (el, active) => {
                el.setAttribute("data-active", active ? "true" : "false");
                el.setAttribute("aria-hidden", active ? "false" : "true");
            };

            const adjustHeight = () => {
                const activeCard = root.querySelector(
                    '.ts-slide[data-active="true"] .ts-card',
                );
                if (!activeCard) return;
                track.style.setProperty(
                    "--ts-h",
                    activeCard.getBoundingClientRect().height + "px",
                );
            };

            slides.forEach((el, i) => setActive(el, i === 0));
            adjustHeight();

            if (slides.length <= 1) return;

            let idx = 0;
            const interval = Math.max(
                2400,
                Number(root.getAttribute("data-interval") || 5200),
            );
            let timer = null;
            let stopped = false; // stop autoplay after user interaction
            let hovered = false;

            const tick = () => {
                if (!hovered && !stopped) next();
            };
            const start = () => {
                stop();
                timer = setInterval(tick, interval);
            };
            const stop = () => {
                if (timer) clearInterval(timer);
                timer = null;
            };

            const next = () => {
                const prev = slides[idx];
                idx = (idx + 1) % slides.length;
                const cur = slides[idx];
                setActive(prev, false);
                setActive(cur, true);
                adjustHeight();
            };

            // Stop autoplay on user intent
            const stopForUser = () => {
                if (stopped) return;
                stopped = true;
                stop();
            };
            ["pointerdown", "touchstart", "wheel", "keydown"].forEach((ev) => {
                root.addEventListener(ev, stopForUser, { passive: true });
            });

            root.addEventListener("mouseenter", () => (hovered = true));
            root.addEventListener("mouseleave", () => (hovered = false));

            // init
            setTimeout(start, 200);
            window.addEventListener("resize", adjustHeight);
        }

        document.readyState === "loading"
            ? document.addEventListener(
                  "DOMContentLoaded",
                  () => document.querySelectorAll(".ts-wrap").forEach(boot),
                  { once: true },
              )
            : document.querySelectorAll(".ts-wrap").forEach(boot);
    })();
</script>
