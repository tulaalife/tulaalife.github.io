---
import YogaListing from "../../components/YogaListing.astro";
import { yogaPlans } from "../../data/generated";
import { YOGA_UI } from "../../utils/ui-strings";
import type { GetStaticPaths } from "astro";

export const getStaticPaths = (async ({ paginate }) => {
    const englishPlans = yogaPlans.filter((p) => p.language === "en");
    return paginate(englishPlans, { pageSize: 20 });
}) satisfies GetStaticPaths;

const { page } = Astro.props;
const lang = "en";
const labels = YOGA_UI.en;

// --- NEW SORTING LOGIC ---
// Get the goal from the URL (e.g., ?goal=medical)
const url = new URL(Astro.request.url);
const currentGoal = url.searchParams.get("goal");

const CATEGORY_MAP: Record<string, string[]> = {
    general: ["pain_relief", "stress_relief", "fitness_posture"],
    seniors: ["seniors", "pain_relief"],
    medical: ["yoga_therapy"],
    womens: ["womens_health"],
};

if (currentGoal && CATEGORY_MAP[currentGoal]) {
    const targetGoals = CATEGORY_MAP[currentGoal];

    // Sort the existing page data so matches are at the top
    page.data.sort((a, b) => {
        const aMatch = targetGoals.some((g) => a.goals?.includes(g));
        const bMatch = targetGoals.some((g) => b.goals?.includes(g));

        if (aMatch && !bMatch) return -1;
        if (!aMatch && bMatch) return 1;
        return 0;
    });
}
// -------------------------

const pageTitle = `${labels.title} · ${labels.metaPage} ${page.currentPage}`;
const canonical =
    page.currentPage === 1 ? `/yoga` : `/yoga/${page.currentPage}`;
---

<YogaListing
    page={page}
    title={pageTitle}
    description={labels.description}
    canonicalPath={canonical}
    lang={lang}
    labels={labels}
/>
