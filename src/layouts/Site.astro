---
import FooterCard from "../components/FooterCard.astro";
import { ClientRouter } from "astro:transitions";
import "../styles/global.css"; // Driving global design and typography variables tokens from our common CSS file

interface Props {
    title: string;
    description?: string;
    canonicalPath?: string;
    ogImage?: string;
    hideFooter?: boolean;
    hasTranslation?: boolean;
    isLandingPage?: boolean;
}

const {
    title,
    description = "Your private sanctuary for holistic wellness. Yoga, meditation, and tracking—offline and free.",
    canonicalPath = "/",
    ogImage = "https://images.tulaa.life/logo.webp",
    hideFooter = false,
    hasTranslation = true,
    isLandingPage = false,
} = Astro.props;

const site = "https://tulaalife.com";
const currentLang = Astro.currentLocale || "en";
const currentPath = Astro.url.pathname;
const basePath = canonicalPath.startsWith("/")
    ? canonicalPath
    : `/${canonicalPath}`;
const enUrl = new URL(basePath, site).toString();
const hiUrl = new URL(
    basePath === "/" ? "/hi" : `/hi${basePath}`,
    site,
).toString();
const currentUrl = new URL(currentPath, site).toString();
const socialImage = ogImage.startsWith("http")
    ? ogImage
    : new URL(ogImage, site).toString();

const ui = {
    en: { privacy: "Privacy", terms: "Terms", contact: "Contact" },
    hi: { privacy: "गोपनीयता", terms: "शर्तें", contact: "संपर्क" },
};
const t = ui[currentLang as keyof typeof ui] || ui.en;
const getLink = (path: string) =>
    currentLang === "en" ? path : path === "/" ? "/hi" : `/hi${path}`;

let targetUrl = "/";
const cleanPath = currentPath.replace(/\/$/, "") || "/";
if (currentLang === "en") {
    const pathWithoutEn = cleanPath.replace(/^\/en(\/|$)/, "/");
    targetUrl = pathWithoutEn === "/" ? "/hi" : `/hi${pathWithoutEn}`;
} else {
    targetUrl = cleanPath.replace(/^\/hi(\/|$)/, "/") || "/";
}
targetUrl = targetUrl.replace(/\/+/g, "/");
---

<!doctype html>
<html
    lang={currentLang}
    data-theme="light"
    class={isLandingPage ? "is-landing" : ""}
>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="color-scheme" content="light dark" />
        <meta name="theme-color" content="#ffffff" id="theme-color-meta" />
        <title>{title}</title>
        <meta name="description" content={description} />
        <link rel="canonical" href={currentUrl} />
        <meta name="robots" content="index,follow" />

        {
            hasTranslation && (
                <>
                    <link rel="alternate" hreflang="en" href={enUrl} />
                    <link rel="alternate" hreflang="hi" href={hiUrl} />
                    <link rel="alternate" hreflang="x-default" href={enUrl} />
                </>
            )
        }

        <meta property="og:type" content="website" />
        <meta property="og:url" content={currentUrl} />
        <meta property="og:title" content={title} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={socialImage} />

        <link rel="icon" type="image/webp" href="/assets/icons/logo_64.webp" />
        <ClientRouter />

        <!-- Google Fonts for Hindi (Noto Sans Devanagari) -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Noto+Sans+Devanagari:wght@400;500;600;700&display=swap"
            rel="stylesheet"
        />

        <script is:inline>
            (() => {
                const root = document.documentElement;
                const metaColor = document.getElementById("theme-color-meta");
                const bgColors = { light: "#ffffff", dark: "#18181b" };

                function getSystemTheme() {
                    return window.matchMedia("(prefers-color-scheme: dark)")
                        .matches
                        ? "dark"
                        : "light";
                }

                function setTheme(theme) {
                    root.dataset.theme = theme;
                    root.style.colorScheme = theme;
                    if (metaColor)
                        metaColor.setAttribute("content", bgColors[theme]);
                }

                function init() {
                    const hasBeenRedirected = localStorage.getItem(
                        "tulaa-lang-detected",
                    );
                    const isHomePage =
                        window.location.pathname === "/" ||
                        window.location.pathname === "";
                    if (!hasBeenRedirected && isHomePage) {
                        const userLang =
                            navigator.language || navigator.userLanguage;
                        if (userLang.startsWith("hi")) {
                            localStorage.setItem("tulaa-lang-detected", "true");
                            window.location.href = "/hi";
                            return;
                        }
                        localStorage.setItem("tulaa-lang-detected", "true");
                    }
                    const savedTheme = localStorage.getItem("tulaa-theme");
                    setTheme(savedTheme || getSystemTheme());
                }

                window
                    .matchMedia("(prefers-color-scheme: dark)")
                    .addEventListener("change", (e) => {
                        if (!localStorage.getItem("tulaa-theme"))
                            setTheme(e.matches ? "dark" : "light");
                    });
                init();
                document.addEventListener("astro:page-load", init);

                window.toggleTheme = function () {
                    const next =
                        root.dataset.theme === "dark" ? "light" : "dark";
                    setTheme(next);
                    localStorage.setItem("tulaa-theme", next);
                };
            })();
        </script>

        <link
            rel="preload"
            as="font"
            type="font/woff2"
            href="/fonts/InterVariable.woff2"
            crossorigin
        />

        <style>
            @font-face {
                font-family: "Inter Var";
                src: url("/fonts/InterVariable.woff2") format("woff2");
                font-weight: 100 900;
                font-display: swap;
            }
        </style>

        <style is:global>
            /* ONLY lock scrolling if on the landing page layout view */
            html.is-landing,
            html.is-landing body {
                height: 100%;
                overflow: hidden;
            }

            /* Optical adjustments for buttons dependent on layout context */
            :lang(hi) .btn-pill,
            :lang(hi) .btn-sm,
            :lang(hi) .filter-btn {
                padding-top: 10px;
                padding-bottom: 8px;
            }

            a {
                color: inherit;
                text-decoration: none;
            }

            /* --- SCOPED SNAP SCROLLING ARCHITECTURE --- */
            html.is-landing .scroll-container {
                height: 100dvh;
                width: 100%;
                overflow-y: scroll;
                scroll-snap-type: y mandatory;
                scroll-behavior: smooth;
                scrollbar-width: none;
            }
            html.is-landing .scroll-container::-webkit-scrollbar {
                display: none;
            }

            html.is-landing .scroll-section {
                height: 100dvh;
                width: 100%;
                scroll-snap-align: start;

                /* ADD THIS LINE 👇 */
                scroll-snap-stop: always;

                display: flex;
                align-items: center;
                justify-content: center;
                position: relative;
                overflow: hidden;
                padding-top: var(--header-height);
            }

            /* --- FALLBACK FOR NORMAL PAGES --- */
            html:not(.is-landing) .scroll-container {
                padding-top: var(--header-height);
                min-height: 100vh;
            }
            html:not(.is-landing) .normal-footer {
                margin-top: 60px;
                padding-bottom: 40px;
            }

            /* --- SCROLL DRIVEN LAYOUT ANIMATIONS --- */
            .animate-on-scroll {
                opacity: 0;
                transform: translateY(30px);
                transition:
                    opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1),
                    transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
            }
            .delay-1 {
                transition-delay: 0.15s;
            }
            .delay-2 {
                transition-delay: 0.3s;
            }

            .is-visible .animate-on-scroll {
                opacity: 1;
                transform: translateY(0);
            }

            /* Header Specific Layout Styling */
            header {
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                height: var(--header-height);
                z-index: 50;
                background: color-mix(in srgb, var(--bg-main), transparent 15%);
                backdrop-filter: blur(12px);
                border-bottom: 1px solid var(--border);
            }
            header .inner {
                display: flex;
                align-items: center;
                justify-content: space-between;
                height: 100%;
            }
            .logo {
                display: flex;
                align-items: center;
                gap: 10px;
                font-weight: var(--fw-bold);
            }
            .logo img {
                width: 32px;
                height: 32px;
                border-radius: 8px;
            }
            .header-actions {
                display: flex;
                align-items: center;
                gap: 8px;
            }

            .lang-icon {
                font-size: 1rem;
                line-height: 1;
            }
            .lang-toggle {
                display: inline-flex;
                align-items: center;
                gap: 4px;
                padding: 4px 10px;
                font-size: 0.8rem;
                font-weight: 600;
                color: var(--text-main);
                background: none;
                border: 1px solid var(--border);
                border-radius: 99px;
                transition: background 0.2s;
            }
            .lang-toggle:hover {
                background: var(--bg-sub);
            }

            .theme-toggle {
                background: none;
                border: none;
                cursor: pointer;
                color: var(--text-main);
                padding: 8px;
                border-radius: 50%;
                transition: background 0.2s;
            }
            .theme-toggle:hover {
                background: var(--bg-sub);
            }

            /* Footer Card Sub-actions & Toast System */
            .contact-btn {
                background: none;
                border: none;
                font: inherit;
                color: var(--text-muted);
                cursor: pointer;
                transition: color 0.2s ease;
            }
            .contact-btn:hover {
                color: var(--text-main);
                text-decoration: underline;
            }

            .copy-toast {
                position: fixed;
                bottom: 24px;
                left: 50%;
                transform: translateX(-50%) translateY(20px);
                background: color-mix(in srgb, var(--bg-sub), transparent 10%);
                color: var(--text-main);
                padding: 10px 16px;
                border-radius: 999px;
                font-size: 0.85rem;
                border: 1px solid var(--border);
                box-shadow: var(--shadow);
                opacity: 0;
                pointer-events: none;
                transition: all 0.3s ease;
                backdrop-filter: blur(10px);
                z-index: 1000;
            }
            .copy-toast.show {
                opacity: 1;
                transform: translateX(-50%) translateY(0);
            }
        </style>
    </head>

    <body>
        <header>
            <div class="container inner">
                <a href={getLink("/")} class="logo">
                    <img
                        src="https://images.tulaa.life/logo_64.webp"
                        alt="Tulaa Logo"
                    />
                    <span>Tulaa</span>
                </a>
                <div class="header-actions">
                    {
                        hasTranslation && (
                            <a href={targetUrl} class="lang-toggle">
                                <span class="lang-icon">🌐</span>
                                {currentLang === "en" ? "हिंदी" : "EN"}
                            </a>
                        )
                    }
                    <button
                        id="themeBtn"
                        class="theme-toggle"
                        aria-label="Toggle Theme"
                    >
                        <svg
                            xmlns="http://www.w3.org/2000/svg"
                            width="20"
                            height="20"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            ><circle cx="12" cy="12" r="5"></circle><path
                                d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"
                            ></path></svg
                        >
                    </button>
                </div>
            </div>
        </header>

        <main class="scroll-container" id="main-scroll-container">
            <slot />

            {
    !hideFooter && (
        <section
            class={
                isLandingPage ? "scroll-section" : "normal-footer"
            }
            style={
                isLandingPage
                    /* Added position: relative and removed top/bottom padding */
                    ? "min-height: 100dvh; padding: 0; scroll-snap-align: center; scroll-snap-stop: always; position: relative;"
                    : "position: relative;"
            }
        >
            <div 
                class="container" 
                style={isLandingPage ? "display: flex; align-items: center; justify-content: center; height: 100%;" : ""}
            >
                <FooterCard />
                
                <!-- Pin to bottom on landing page, flow normally on other pages -->
                <div 
                    class="legal-wrapper" 
                    style={isLandingPage ? "position: absolute; bottom: 32px; left: 0; right: 0; width: 100%;" : "margin-top: 48px;"}
                >
                    <div
                        class="legal"
                        style="display:flex; gap:24px; justify-content:center;"
                    >
                        <a href={getLink("/legal/privacy")}>
                            {t.privacy}
                        </a>
                        <a href={getLink("/legal/terms")}>{t.terms}</a>
                        <button class="contact-btn" id="contactBtn">
                            {t.contact}
                        </button>
                    </div>
                    <p style="text-align: center; font-size: 0.8rem; margin-top: 16px; color: var(--text-muted);">
                        © {new Date().getFullYear()} Tulaalife Wellness Private Limited
                    </p>
                </div>
            </div>
        </section>
    )
}
        </main>

        <script>
            declare global {
                interface Window {
                    toggleTheme: () => void;
                    copyEmail: () => void;
                }
            }

            window.copyEmail = async function () {
                const email = "contact@tulaalife.com";
                try {
                    await navigator.clipboard.writeText(email);
                } catch (e) {
                    console.error("Clipboard copy failed", e);
                    return;
                }

                const toastId = "copyToast";
                let toast = document.getElementById(toastId);
                const isHindi = document.documentElement.lang === "hi";
                const message = isHindi
                    ? "ईमेल कॉपी हो गया 💛 आप हमें कभी भी लिख सकते हैं"
                    : "Email copied 💛 You can write to us anytime";

                if (!toast) {
                    toast = document.createElement("div");
                    toast.id = toastId;
                    toast.className = "copy-toast";
                    document.body.appendChild(toast);
                }

                if (toast.classList.contains("show")) return;

                toast.innerText = message;
                toast.classList.add("show");

                setTimeout(() => {
                    toast.classList.remove("show");
                }, 2200);
            };

            document.addEventListener("astro:page-load", () => {
                const themeBtn = document.getElementById("themeBtn");
                if (themeBtn) {
                    themeBtn.addEventListener("click", () =>
                        window.toggleTheme(),
                    );
                }

                const contactBtn = document.getElementById("contactBtn");
                if (contactBtn) {
                    contactBtn.addEventListener("click", window.copyEmail);
                }

                const observerOptions = {
                    root: document.getElementById("main-scroll-container"),
                    threshold: 0.2,
                };

                const observer = new IntersectionObserver((entries) => {
                    entries.forEach((entry) => {
                        if (entry.isIntersecting) {
                            entry.target.classList.add("is-visible");
                        }
                    });
                }, observerOptions);

                document
                    .querySelectorAll(".scroll-section, .footer-card-container")
                    .forEach((section) => {
                        observer.observe(section);
                    });
            });
        </script>
    </body>
</html>
